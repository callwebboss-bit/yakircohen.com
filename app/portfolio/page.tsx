import PortfolioPageContent, {
  portfolioMetadata,
} from "@/components/seo/PortfolioPageContent";

export const metadata = portfolioMetadata;

export default function PortfolioPage() {
  return <PortfolioPageContent />;
}
