/** Pre-written WhatsApp-forward message for managers / procurement. */
export function buildCorporateShareText(serviceLabel: string): string {
  return `היי, ראיתי את ${serviceLabel} של יקיר כהן הפקות ונראה לי שזה בדיוק מה שאנחנו צריכים לעסק. שווה להציץ בעמוד שלהם:`;
}
