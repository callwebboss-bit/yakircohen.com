name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: npm audit
        run: npm audit --audit-level=high

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm test

      - name: Audit images
        run: npm run audit:images

      - name: Audit links
        run: npm run audit:links

      - name: Audit headings
        run: npm run audit:headings

      - name: Audit SEO titles
        run: npm run audit:seo-titles

      - name: Audit schema
        run: npm run audit:schema

      - name: Audit orphans
        run: npm run audit:orphans

      - name: Audit sitemap indexable
        run: npm run audit:sitemap-indexable

      - name: Audit AEO coverage
        run: npm run audit:aeo-coverage

      - name: Audit funnel events
        run: npm run audit:funnel-events

      - name: Audit pricing
        run: npm run audit:pricing

      - name: Audit a11y (skip unless RUN_A11Y=1)
        run: npm run audit:a11y

      - name: Build (with Pagefind)
        run: npm run build:full

      - name: Cypress E2E
        run: npm run cy:run
